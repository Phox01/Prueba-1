def fibonacci(a, b, limit, numbers):
    if len(numbers)==limit:
        return numbers
    else:
        
        


def main():
    a=0
    b=1
    numbers=[]
    limit=int(input('Ingrese cuántos números de fibonacci quiere'))
    print(fibonacci(a, b, limit, numbers))
